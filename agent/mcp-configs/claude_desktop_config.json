{
  "mcpServers": {
    "forkcast": {
      "command": "npm",
      "args": ["run", "mcp"],
      "cwd": "/path/to/forkcast-data",
      "env": {
        "FORKCAST_DATA_LATEST_ROOT": "/path/to/forkcast-data/dist/latest"
      }
    }
  }
}
